import { CustomizationSidebar } from '../../CustomizationSidebar';
import type { CustomizationSidebarProps } from '../../customization-sidebar/types';

export function CustomizationSidebarContainer(props: CustomizationSidebarProps) {
  return <CustomizationSidebar {...props} />;
}
